sudo: required
services:
  - docker

before_install:
  - docker build -t nibblefritz/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run nibblefritz/react-test npm test -- --coverage

after_success:
  - docker build -t nibblefritz/multi-client ./client
  - docker build -t nibblefritz/multi-nginx ./nginx
  - docker build -t nibblefritz/multi-server ./server
  - docker build -t nibblefritz/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push nibblefritz/multi-client
  - docker push nibblefritz/multi-nginx
  - docker push nibblefritz/multi-server
  - docker push nibblefritz/multi-worker
